<Window x:Class="Zadanie5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Zadanie5"
        mc:Ignorable="d"
        Title="Rejestr Egzaminatorów" Height="460" Width="440" Background="CadetBlue">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Wyszukaj" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtSzukajId" Margin="0,0,5,0" VerticalContentAlignment="Center"/>
                    <Button x:Name="btnSzukaj" Content="Szukaj po ID" Grid.Column="1" Padding="10,5" Click="BtnSzukaj_Click"/>
                </Grid>
                <TextBlock x:Name="lblWynikSzukania" Margin="0,5,0,0" FontWeight="Bold" TextWrapping="Wrap"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Dodaj Egzaminatora" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Imię:"/>
                    <TextBox x:Name="txtImie" Grid.Column="1" Margin="0,0,5,0" TextChanged="PoleTekstowe_TextChanged"/>
                    <Label Content="Nazwisko:" Grid.Column="2"/>
                    <TextBox x:Name="txtNazwisko" Grid.Column="3" TextChanged="PoleTekstowe_TextChanged"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="cmbSpecjalizacja" Margin="0,0,5,0"/>
                    <Button x:Name="btnDodaj" Content="Dodaj" Grid.Column="1" Padding="10,5" IsEnabled="False" Click="BtnDodaj_Click"/>
                </Grid>
            </StackPanel>
        </GroupBox>

        <ListBox x:Name="lstEgzaminatorzy" Grid.Row="2"/>
    </Grid>
</Window>
